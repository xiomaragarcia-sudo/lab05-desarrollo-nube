version: "3.9"

services:
  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: tienda
      MYSQL_USER: usuario
      MYSQL_PASSWORD: userpass
    ports:
      - "3306:3306"
    volumes:
      - datos_mysql:/var/lib/mysql

volumes:
  datos_mysql:
